/**
 * The habit library browse surface.
 *
 * Was parked while the Practice Protocol was the front door. Now ON: practices
 * and library habits are one catalog, so this screen IS the library — every
 * habit the app offers, in one list. See docs/habit-template-unification.md.
 *
 * Kept as a flag only until Phase 5 finishes removing the old split screens;
 * delete it and its call sites after that.
 */
export const SHOW_HABIT_LIBRARY = true;
